{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-ws": "^5.0.2",
    "fs": "^0.0.1-security"
  },
  "scripts": {
    "start": "npx ts-node src/server.ts",
    "start-production": "node build/index.js",
    "build": "rm -rf ./build && tsc",
    "deploy": "export NODE_ENV=production && yarn run build && gcloud config set project silly-side-projects && gcloud builds submit --tag gcr.io/silly-side-projects/temps-backend && gcloud run deploy temps-backend --image gcr.io/silly-side-projects/temps-backend --platform managed --region us-east1"
    
  },
  "devDependencies": {
    "@types/cors": "^2.8.15",
    "@types/express": "^4.17.20",
    "@types/express-ws": "^3.0.3",
    "@types/ws": "^8.5.8",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
